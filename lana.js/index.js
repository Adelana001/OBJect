// var girl = 10.2;
        // console.log(girl);
        var a = 20;
        var b = 3;
        var initialRand = Math.random();
        var maxLimit = 6
        var minLimit = 1
        var randomNum = initialRand* maxLimit
        var finalNumber = Math.floor(randomNum) + minLimit
        console.log(finalNumber,randomNum)
        
     //    console.log(Math.floor(a/b));
     //    console.log(Math.round(a/b));
     //    console.log((a%b));


        